# Claude Design reference

`Canvas Sweeper.dc.html` and `support.js` are retained verbatim as the visual and interaction reference for Canvas Sweeper. They are not loaded, bundled, or otherwise used by the production Next.js runtime. `support.js` is the prototype-only Claude Design runtime.

The original root copies are also preserved so the supplied files have not been deleted or overwritten.
